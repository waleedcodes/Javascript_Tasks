// param => expression

// (param) => expression

// (param1, paramN) => expression;

// param => {
//   statements
// }

// (param1, paramN) => {
//   statements
// }

// Example Arror Method 

// ( a, b, ...r ) => expression;
//     ( a = 400, b = 20, c ) => expression;
//         ( [a, b] = [10, 20] ) => expression;
//             ( { a, b } = { a: 10, b: 20 } ) => expression;